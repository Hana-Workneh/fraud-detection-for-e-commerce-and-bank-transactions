## Scripts

These scripts mirror the notebook steps to make preprocessing reproducible.

- `task1_preprocess_fraud_data.py`: cleaning + IP-to-country merge + feature engineering (Task 1)
- `task1_preprocess_creditcard.py`: cleaning checks + basic feature prep (Task 1)
